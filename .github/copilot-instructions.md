Refer to `AGENTS.md` in the repo root as the canonical guide for this codebase.

When editing or reviewing, print real command output whenever you mention a command result.

Use verbatim comparison, not heading-matching, when checking whether a file or doc already covers a specific requirement.
